#include "main.h"
#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include <stdarg.h>

int _printf(const char *format, ...)
{
	va_list parameters;
	int = 0;
	char *p;

	va_start(parameters, format);
	while (format[i])
	{
		if(format[i] = %)
		{

		switch (format[i])
		{
			case '
	}
